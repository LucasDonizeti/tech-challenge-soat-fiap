apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  labels:
    app: oficina-api
spec:
  selector:
    app: oficina-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: NodePort